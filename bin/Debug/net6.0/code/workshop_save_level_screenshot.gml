//Unused because for some reason changing this script broke everything. Probably a UMLib or GMML bug.
if(global.setting_custom_thumbnails){
    if(!global.has_set_thumbnail){
        #orig#()
    }
} else {
    #orig#()
}